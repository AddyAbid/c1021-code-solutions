/* exported pick */
/*

*/

function pick(source, keys) {
  var newObj = {};

  for (var key in source) {
    for (var i = 0; i < keys.length; i++) {
      var value = keys[i];
    }
  }
  if (key === value) {

    newObj[key] = source[keys[i]];

    return newObj;
  }

}
